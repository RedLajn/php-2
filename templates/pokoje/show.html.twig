{% extends 'base.html.twig' %}

{% block title %}Pokoje{% endblock %}

{% block body %}
    <h1>Pokoje</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pokoje.id }}</td>
            </tr>
            <tr>
                <th>Wymiar</th>
                <td>{{ pokoje.Wymiar }}</td>
            </tr>
            <tr>
                <th>Cena</th>
                <td>{{ pokoje.Cena }}</td>
            </tr>
            <tr>
                <th>Przeznaczenie</th>
                <td>{{ pokoje.Przeznaczenie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pokoje_index') }}">Powrót do lsity</a>

    <a href="{{ path('app_pokoje_edit', {'id': pokoje.id}) }}">edit</a>

    {{ include('pokoje/_delete_form.html.twig') }}
{% endblock %}
